server:
  port: 5025

spring:
  application:
    name: verification-service
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 1234
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  mail:
    host: ${SMTP:smtp.gmail.com}
    port: ${PORT:587}
    username: chat.hivein@gmail.com
    password: cfhqzucqsffxtuly
    protocol: smtp
    properties.mail.smtp:
      auth: true
      starttls.enable: true
      ssl.trust: smtp.gmail.com

site:
  base:
    url:
      https: ${SITE_URL:http://localhost:8080}

security:
  jwt:
    expiration: ${TOKEN_EXPIRATION:3600}
    secret: ${JWT_SECRET:sekretnayabomba}

feign:
  client:
    config:
      default:
        readTimeout: 1500
        connectTimeout: 500
userData:
  service:
    base:
      url: ${SERVICE_URL:https://hivein-userdata.herokuapp.com}
    name: ${SERVICE_NAME:userData}

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URI:https://hivein-registry.herokuapp.com/eureka}
    instance:
      hostname: ${HOSTNAME:hivein-registry}

